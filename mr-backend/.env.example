PORT=8787
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:8080   # 你的 Unity/前端调试源
DEFAULT_MODEL=gpt-4o-mini
REALTIME_MODEL=gpt-4o-realtime-preview
.env copyTTS_MODEL=gpt-4o-mini-tts          # 也可用 tts-1 / tts-1-hd（看账号可用模型）
STT_MODEL=gpt-4o-transcribe        # 也可用 whisper-1（按需）
OPENAI_API_KEY=XXXX